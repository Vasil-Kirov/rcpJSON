module main;

#import json;
#import mem;
#import os;
_ :: #import io;

test_string :: #embed_str "../test.json";
//test_string :: "{\"v\": \"\"}";

struct Test {
	name: string,
	version: int,
	enabled: bool,
	timeout: f64,
	tags: []string,
	settings: struct {
		retryCount: int,
		logLevel: string,
		output: string,
	},
	items: [] struct {
		id: int,
		value: string,
	},
};

struct Stuff {
	x: []f64,
	y: [4]int,
	z: string,
	w: struct {
		f: [2]string
	}
};

main :: fn() -> i32 {
	scratch := mem.create_scratch(mem.mb(1));
	defer mem.destroy_scratch(&scratch);

	s := Stuff {
		x = .{1.23, 4.54, 2.2002},
		y = .{3, 2, 1},
		z = "321s123",
		w = .{
			f = .{"foo", "bar"}
		}
	};

	js := json.into_json(s, &scratch);
	str := json.print(js, &scratch);
	println("out: %", str);



	//obj := json.parse(test_string, &scratch);
	//t := Test{};
	//if json.read_into(obj, &t, Test) {
	//	println("Success: %", t);
	//}
	//else {
	//	println("Failed!");
	//}


    return 0;
}


