module build;

c   :: #import compile;
arr :: #import array;
_   :: #import io;
#import mem
#import str

files :: []string { "main.rcp", "json.rcp" };

compile :: fn() -> c.CompileInfo {
    return .{
		source_dirs = .{"../src/"},
		flags = .DebugInfo,
        files = files

    };
}

after_link :: fn(objs: []string) {
	println("Done!");
}

